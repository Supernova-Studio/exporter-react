{[ const types = ["color","border","gradient","measure","shadow","radius","typography"] /]}

{[ for type in types ]}
{[ const behavior = @js.getBehavior(type) /]}
import { {{ behavior.varName }} } from './{{ behavior.fileName }}';
{[/]}

export const Theme = {
    {[ for type in types ]}
    {[ const behavior = @js.getBehavior(type) /]}
    {{ behavior.themeProperty }}: {{ behavior.varName }},
    {[/]}
};